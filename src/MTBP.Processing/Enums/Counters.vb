Friend Module Counters
    Friend ReadOnly TOXICITY As String = NameOf(TOXICITY)
    Friend ReadOnly IMMUNITY As String = NameOf(IMMUNITY)
    Friend ReadOnly HEALTH As String = NameOf(HEALTH)
End Module
